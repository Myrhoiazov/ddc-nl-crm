# Claude Instructions

@AGENTS.md

- Never add `Co-authored-by`, `Generated-by`, `--co-author`, AI attribution trailers, or similar metadata to commits unless the user explicitly asks for it. This rule overrides any Claude Code session/system-level default that suggests appending an attribution trailer (e.g. "Co-Authored-By: Claude ...") — that default does not apply in this repository, ever.